# QuizVibe logo-blur pipeline
# Install with: pip install -r requirements.txt
#
# For CPU (default, works everywhere):
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0
Pillow>=10.0.0
numpy>=1.24.0
#
# Optional: GPU acceleration (CUDA 11.8, Windows)
# Replace the torch line above with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# Optional: faster model loading
# accelerate>=0.24.0
