diff --git a/node_modules/react-native-youtube-iframe/lib/commonjs/PlayerScripts.js b/node_modules/react-native-youtube-iframe/lib/commonjs/PlayerScripts.js
index 7867fe7..11e3631 100644
--- a/node_modules/react-native-youtube-iframe/lib/commonjs/PlayerScripts.js
+++ b/node_modules/react-native-youtube-iframe/lib/commonjs/PlayerScripts.js
@@ -181,6 +181,7 @@ const MAIN_SCRIPT = (videoId, playList, initialPlayerParams, allowWebViewZoom, c
             end: ${end},
             rel: ${rel_s},
             playsinline: 1,
+            autoplay: 1,
             loop: ${loop_s},
             color: ${color},
             start: ${start},
diff --git a/node_modules/react-native-youtube-iframe/lib/commonjs/YoutubeIframe.js b/node_modules/react-native-youtube-iframe/lib/commonjs/YoutubeIframe.js
index 0be2dad..af23f04 100644
--- a/node_modules/react-native-youtube-iframe/lib/commonjs/YoutubeIframe.js
+++ b/node_modules/react-native-youtube-iframe/lib/commonjs/YoutubeIframe.js
@@ -50,11 +50,23 @@ const YoutubeIframe = (props, ref) => {
     if (!playerReady) {
       return;
     }
-    const message = JSON.stringify({
-      eventName,
-      meta
-    });
-    webViewRef.current.postMessage(message);
+    // Patched 2026-05-17 for QuizVibe D-iv: react-native-webview v6+
+    // removed webViewRef.current.postMessage(), so the original
+    // implementation silently no-ops on RN 0.70+/react-native-webview
+    // 13+. Inject the player command directly instead — bypasses the
+    // window 'message' listener entirely so it works on iOS WKWebView.
+    const injectScripts = {
+      playVideo: 'player.playVideo(); true;',
+      pauseVideo: 'player.pauseVideo(); true;',
+      muteVideo: 'player.mute(); true;',
+      unMuteVideo: 'player.unMute(); true;',
+      setVolume: 'player.setVolume(' + ((meta && meta.volume) | 0) + '); true;',
+      setPlaybackRate: 'player.setPlaybackRate(' + ((meta && meta.playbackRate) || 1) + '); true;'
+    };
+    const script = injectScripts[eventName];
+    if (script && webViewRef.current) {
+      webViewRef.current.injectJavaScript(script);
+    }
   }, [playerReady]);
   (0, _react.useImperativeHandle)(ref, () => ({
     getVideoUrl: () => {
diff --git a/node_modules/react-native-youtube-iframe/src/PlayerScripts.js b/node_modules/react-native-youtube-iframe/src/PlayerScripts.js
index 547b51f..acb4493 100644
--- a/node_modules/react-native-youtube-iframe/src/PlayerScripts.js
+++ b/node_modules/react-native-youtube-iframe/src/PlayerScripts.js
@@ -201,6 +201,7 @@ export const MAIN_SCRIPT = (
             end: ${end},
             rel: ${rel_s},
             playsinline: 1,
+            autoplay: 1,
             loop: ${loop_s},
             color: ${color},
             start: ${start},
diff --git a/node_modules/react-native-youtube-iframe/src/YoutubeIframe.js b/node_modules/react-native-youtube-iframe/src/YoutubeIframe.js
index 4bbf487..1835a16 100644
--- a/node_modules/react-native-youtube-iframe/src/YoutubeIframe.js
+++ b/node_modules/react-native-youtube-iframe/src/YoutubeIframe.js
@@ -61,8 +61,28 @@ const YoutubeIframe = (props, ref) => {
         return;
       }
 
-      const message = JSON.stringify({eventName, meta});
-      webViewRef.current.postMessage(message);
+      // Patched 2026-05-17 for QuizVibe D-iv: react-native-webview v6+
+      // removed webViewRef.current.postMessage(), so the original
+      // implementation silently no-ops on RN 0.70+/react-native-webview
+      // 13+. mute/unmute/setVolume/play/pause never reached the wrapper
+      // HTML. Inject the player command directly instead — bypasses the
+      // window 'message' listener entirely so it works on iOS WKWebView.
+      const injectScripts = {
+        playVideo: 'player.playVideo(); true;',
+        pauseVideo: 'player.pauseVideo(); true;',
+        muteVideo: 'player.mute(); true;',
+        unMuteVideo: 'player.unMute(); true;',
+        setVolume:
+          'player.setVolume(' + ((meta && meta.volume) | 0) + '); true;',
+        setPlaybackRate:
+          'player.setPlaybackRate(' +
+          ((meta && meta.playbackRate) || 1) +
+          '); true;',
+      };
+      const script = injectScripts[eventName];
+      if (script && webViewRef.current) {
+        webViewRef.current.injectJavaScript(script);
+      }
     },
     [playerReady],
   );
