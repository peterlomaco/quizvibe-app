diff --git a/node_modules/@ptomasroos/react-native-multi-slider/MultiSlider.js b/node_modules/@ptomasroos/react-native-multi-slider/MultiSlider.js
index f3114aa..cce1862 100644
--- a/node_modules/@ptomasroos/react-native-multi-slider/MultiSlider.js
+++ b/node_modules/@ptomasroos/react-native-multi-slider/MultiSlider.js
@@ -224,6 +224,19 @@ export default class MultiSlider extends React.Component {
         ? this.props.minMarkerOverlapDistance
         : this.stepLength);
     var top = this.props.sliderLength;
+    // PATCH (QuizVibe): per-thumb floor för höger marker. När minMarkerValueTwo
+    // är satt får to-thumben inte dras under det värdet (era to-year >= 1980).
+    // Höj bottom till golvets pixel-position så thumben FYSISKT stoppar mid-drag.
+    if (this.props.minMarkerValueTwo != null) {
+      var floorPositionTwo = valueToPosition(
+        this.props.minMarkerValueTwo,
+        this.optionsArray,
+        this.props.sliderLength,
+      );
+      if (floorPositionTwo > bottom) {
+        bottom = floorPositionTwo;
+      }
+    }
     var confined =
       unconfined < bottom ? bottom : unconfined > top ? top : unconfined;
     var slipDisplacement = this.props.touchDimensions.slipDisplacement;
diff --git a/node_modules/@ptomasroos/react-native-multi-slider/index.d.ts b/node_modules/@ptomasroos/react-native-multi-slider/index.d.ts
index 678df51..5fdd02a 100644
--- a/node_modules/@ptomasroos/react-native-multi-slider/index.d.ts
+++ b/node_modules/@ptomasroos/react-native-multi-slider/index.d.ts
@@ -72,6 +72,9 @@ export interface MultiSliderProps {
     markerOffsetX?: number;
     markerOffsetY?: number;
     minMarkerOverlapDistance?: number;
+    /** PATCH (QuizVibe): per-thumb floor för höger marker — to-thumben kan ej
+     *  dras under detta värde mid-drag (t.ex. era to-year >= 1980). */
+    minMarkerValueTwo?: number;
     imageBackgroundSource?: string;
     enableLabel?: boolean;
     vertical?: boolean;
